import React from 'react'
import {SafeAreaView, Text, Button, StatusBar} from 'react-native'

export function FridgeScreens ([]) {
    return(
        <SafeAreaView>
            <StatusBar backgroundColor='#fff' barStyle='#000' />
            
        </SafeAreaView>
    )
}